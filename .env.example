# Set this to your deployed backend URL when running the frontend in production.
# Vercel: add VITE_API_URL as an environment variable in the project settings.
VITE_API_URL=https://your-render-service.onrender.com
